# SFTP Connection Configuration for Local Pipeline
# =================================================
# This file shows example configuration for SFTP authentication.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: .env file is in .gitignore and will NOT be committed to git
#
# Usage: cp .env.example .env
#        chmod 600 .env

# === Required Settings ===

# SFTP server hostname or IP address
SFTP_HOST="sftp.example.com"

# SFTP server port (usually 22)
SFTP_PORT=22

# SFTP username
SFTP_USERNAME="your_username"


# === Authentication Method (choose ONE) ===

# --- Option 1: Private Key Authentication (RECOMMENDED) ---
# Path to your SSH private key file
# Supports: RSA, Ed25519, ECDSA key formats
# Can use absolute path or ~ for home directory
SFTP_PRIVATE_KEY_PATH="~/.ssh/id_rsa"

# Optional: Passphrase for encrypted private key
# Leave empty or comment out if your key is not encrypted
SFTP_PRIVATE_KEY_PASSPHRASE="your_key_passphrase"


# --- Option 2: Password Authentication ---
# Plain text password (less secure than private key)
# Only used if SFTP_PRIVATE_KEY_PATH is not set
# SFTP_PASSWORD="your_password"


# === Authentication Priority ===
# 1. If SFTP_PRIVATE_KEY_PATH is set → Use private key authentication
# 2. Otherwise, if SFTP_PASSWORD is set → Use password authentication
# 3. If neither is set → Connection will fail


# === Examples ===

# Example 1: RSA key without passphrase
# SFTP_HOST="sftp.example.com"
# SFTP_PORT=22
# SFTP_USERNAME="john"
# SFTP_PRIVATE_KEY_PATH="~/.ssh/id_rsa"

# Example 2: Ed25519 key with passphrase
# SFTP_HOST="sftp.example.com"
# SFTP_PORT=22
# SFTP_USERNAME="john"
# SFTP_PRIVATE_KEY_PATH="~/.ssh/id_ed25519"
# SFTP_PRIVATE_KEY_PASSPHRASE="my_secure_passphrase"

# Example 3: Password authentication (not recommended for production)
# SFTP_HOST="sftp.example.com"
# SFTP_PORT=22
# SFTP_USERNAME="john"
# SFTP_PASSWORD="my_password"


# === Security Best Practices ===
# 1. Always use private key authentication when possible
# 2. Protect your private key: chmod 600 ~/.ssh/id_rsa
# 3. Protect this .env file: chmod 600 .env
# 4. Never commit .env to git (already in .gitignore)
# 5. Use read-only SFTP account if possible
# 6. Rotate credentials regularly


# === Troubleshooting ===
# If connection fails, check:
# 1. File permissions: ls -l ~/.ssh/id_rsa (should be -rw-------)
# 2. Key format: file ~/.ssh/id_rsa (should show key type)
# 3. Network: ssh -p 22 username@sftp.example.com
# 4. Logs: cat logs/log_local_sftp.log
