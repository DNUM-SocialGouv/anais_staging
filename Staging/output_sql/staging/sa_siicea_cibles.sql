CREATE TABLE IF NOT EXISTS sa_siicea_cibles (
    finess TEXT,
    finess_ej TEXT,
    rpps VARCHAR,
    siret TEXT,
    code_ape VARCHAR,
    code_uai VARCHAR,
    groupe_cibles VARCHAR,
    nom_cible VARCHAR,
    code_departement TEXT,
    commune VARCHAR,
    adresse VARCHAR,
    nb_mission_realisees INTEGER,
    nb_mission_abandonnees INTEGER,
    nb_decisions INTEGER
)